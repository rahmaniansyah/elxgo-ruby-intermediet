<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../style/index.css">
    <link rel="stylesheet" href="../style/form.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.8.2/css/all.min.css">
    <title>elxgo - List Customer</title>
</head>
<body>
    <div class="container">
        <div class="card">
            <h1 class="text-center">CUSTOMER</h1>
            <hr>
                <div class="menu">
                    <a href="/menu"> Menu</a>
                    <a href="/category"> Category</a>
                    <a href="/customer"> Customer</a>
                    <a href="/order"> <i class="fas fa-shopping-cart"></i></a>
                </div>
            <hr>
            <div class="card-body">
                <div class="menu-header">
                    <a href="/customer/new" class="btn"> + add customer</a>
                    <div class="form-style-2">
                        <form action="/customer" method="get">
                            <input type="text" class="input-field" name="find" placeholder="Find customer..."/>
                            <input type="submit" value="Search" />
                        </form>
                    </div>
                </div>
                <table class="table-styled">
                    <tbody>
                    <% unless customers.empty? %>
                        <% customers.each do |customer| %>
                            <tr>
                                <td class="td-name">
                                    <div class="title"><a href="/customer/<%= customer.id %>/show"><%= customer.name %></a></div>
                                </td>
                                <td><a href="/customer/<%= customer.id %>/edit" class="btn"><i class="fa fa-edit"></i></a></td>
                                <td><a class="btn" onclick="deleteConfirmation(<%= customer.id %>)"><i class="fa fa-trash"></i></a></td>
                            </tr>
                        <% end %>
                    <% else %>
                        <tr><td>No customer avaliable!</td></tr>
                    <% end %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script>
    function deleteConfirmation(id) {
        if (confirm("Are you sure?")) {
            window.location.replace('/customer/'+id+'/delete');
        }
        return false;
    }
</script>   
</body>
<footer>
    <%# <a href='https://www.freepik.com/photos/background'>Background photo created by freepik - www.freepik.com</a> %>
</footer>
</html>